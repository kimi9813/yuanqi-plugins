version: "3.8"

services:
  yuanqi-plugins:
    build: .
    container_name: yuanqi-plugins
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
